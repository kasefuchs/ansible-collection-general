---
- name: Render AmneziaWG config template
  notify:
    - Enable AmneziaWG service
    - Start AmneziaWG service
    - Reload AmneziaWG service
  ansible.builtin.template:
    src: awg.conf.j2
    dest: "{{ amneziawg_config_file }}"
    mode: "0600"
    owner: root
    group: root
